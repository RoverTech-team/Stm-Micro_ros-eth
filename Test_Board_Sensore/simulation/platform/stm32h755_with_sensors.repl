using "./nucleo_h755zi_q_dual.repl"

// Sensor simulations reuse the shared board model so the ultrasonic flow runs
// on the same LED/button/UART/Ethernet-capable platform as the general board
// bring-up scripts.
